import NavBar from "./NavBar"

const Hero = () => {
  return (
    <header className="h-screen bg-hero-pattern bg-center bg-no-repeat bg-cover bg-">
      <NavBar />
    </header>
  )
}

export default Hero